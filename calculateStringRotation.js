'use strict';

function shiftedDiff (first,second) {
	let firstLet = first.charAt(0);
	let firstArr = first.split(firstLet);
	// let secondArr = second.split("");
	// firstArr.sort();
	// secondArr.sort();
	console.log(firstArr);
};

shiftedDiff("eecoff", "coffee");
